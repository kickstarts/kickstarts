'use strict';

/* Directives */

var mainDirective = angular.module('main.directives', []);

mainDirective.directive('', function () {
    return {
        link: function (scope, element, attrs) {
            // Manipulate Dom Events here
        }
    };
});
